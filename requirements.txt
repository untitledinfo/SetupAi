# Core
torch>=2.2
# 4.57.0 is the first PyPI release with Qwen3-Omni support
# (model_type "qwen3_omni_moe" / Qwen3OmniMoeForConditionalGeneration).
# Older transformers can still load plain text models (Qwen2/Qwen3/Llama/...)
# fine, but will crash on the default FIREWING model with:
#   "Unrecognized configuration class ... for this kind of AutoModel"
transformers>=4.57.0
accelerate>=0.30
pyyaml>=6.0
huggingface_hub>=0.24   # used by `setup-ai model list` / `model search`

# Multimodal (audio/video) support for the default Qwen3-Omni model.
# Also requires the `ffmpeg` system package to be installed separately
# (apt install ffmpeg / brew install ffmpeg).
qwen-omni-utils>=0.0.8
soundfile>=0.12

# API
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6

# Optional (int8/int4 quantization) — comment out on CPU-only VPS
# if the packages fail to build; quantized load paths will then be
# unavailable and `setup-ai doctor` will note it.
bitsandbytes>=0.43; sys_platform == "linux"

# Dev / testing
pytest>=8.0
httpx>=0.27
