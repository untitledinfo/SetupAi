# Extra dependencies for scripts/training/ only — not needed to run
# the FIREWING API/CLI itself. Install with:
#   pip install -r requirements.txt -r requirements-training.txt

peft>=0.10
datasets>=2.18
trl>=0.8          # optional, for future SFT/DPO trainer support
accelerate>=0.30
deepspeed>=0.14; sys_platform == "linux"   # optional, for multi-GPU training
